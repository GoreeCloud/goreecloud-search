# GoreeCloud Search Development runtime configuration.
# Copy to ~/.config/goreecloud/search.env and set permissions to 0600.
# Never commit the active file or a real credential.

BRAVE_SEARCH_API_KEY=
