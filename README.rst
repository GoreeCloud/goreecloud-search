GoreeCloud Search
=================

The authoritative repository overview and lifecycle record is ``README.md``.

This reStructuredText file is retained only because the transitional SearXNG-derived packaging and legacy validation paths still consume ``README.rst``. It is not a second product record and must not be used for independent lifecycle, architecture, deployment, or Stable claims.
